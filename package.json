{
  "name": "hunarsangam-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "flutter run -d web-server --web-port 3000 --web-hostname 0.0.0.0",
    "build": "echo 'Flutter project ready for build'"
  }
}
