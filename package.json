{
  "name": "hunarsangam-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "export PATH=\"$PATH:/opt/flutter/bin:/usr/local/bin\"; flutter run -d web-server --web-port 3000 --web-hostname 0.0.0.0 --no-wasm-dry-run",
    "build": "mkdir -p dist && (test -f build/web/index.html && cp -r build/web/* dist/ || cp -r web/* dist/) && test -f dist/index.html"
  }
}
